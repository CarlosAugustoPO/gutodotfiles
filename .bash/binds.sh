# Turn on auto completion when press tab on case insensitive
bind 'set completion-ignore-case on'

# Able to search in history using key up and key down
bind '"\e[A": history-search-backward'
bind '"\e[B": history-search-forward'
